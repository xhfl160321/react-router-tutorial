import React from 'react';
import BigText from '../components/BigText';

const Home = () => {
    return (
        <div>
            <BigText>Home</BigText>
        </div>
    );
};

export default Home;